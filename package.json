{
  "name": "chemlab-sort-react",
  "private": true,
  "version": "105.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "preflight": "node scripts/preflight.mjs",
    "postbuild": "node scripts/postbuild-smoke.mjs",
    "build": "npm run preflight && npm run typecheck && vite build && npm run postbuild",
    "preview": "vite preview"
  },
  "dependencies": {"pixi.js": "^8.0.0"},
  "devDependencies": {"typescript": "^5.9.0", "vite": "^7.0.0"}
}
